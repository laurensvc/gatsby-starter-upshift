import styled from 'styled-components';
import { MAX_CONTENT_WIDTH } from '../constants/general';

const WidthContainer = styled.div`
  margin: auto;
  max-width: ${MAX_CONTENT_WIDTH};
  padding: 60px 10px;
`;

export default WidthContainer;
